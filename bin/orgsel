#!perl

# DATE
# VERSION

use 5.010001;
use strict;
use warnings;

use Perinci::CmdLine::Any;

Perinci::CmdLine::Any->new(
    url => '/App/orgsel/orgsel',
)->run;

# ABSTRACT:
# PODNAME:

=head1 SYNOPSIS

 % orgsel "Headline[level=2]:first-child" ~/o/todo.org


=head1 SEE ALSO

L<Org::Parser>

L<Data::CSel>

L<App::OrgUtils> for other Org-related utilities.

L<htmlsel>, a similar application but for selecting elements of HTML document
tree using CSS selector syntax.

L<jsonsel>, a similar application but for selecting JSON elements using CSel
syntax.

L<yamlsel>, a similar application but for selecting YAML elements using CSel
syntax.
